plate takes a json file, json path and a template, the template contains a string with some form of delimiter like <!--$VARNAME--> that is replaced with values from the json data. if the json data is an array then plate will iterate through it.
